name: Experimental Canonical CI Disabled

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  canonical-disabled:
    name: Experimental canonical gate (not required)
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Canonical CI Gate intentionally absent for V5 substrate experiment."

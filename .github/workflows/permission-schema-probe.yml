# Diagnosis for issue #146:
# main's human-checkpoint publish run scalar is broken because the Python
# triple-quoted TEST_REQUIRED body is dedented to YAML column 0. Keep every
# body line and the closing triple quote inside the run: | indentation.
# Add a real YAML parse oracle before the existing textual contract checks.
name: Permission schema probe
on:
  push:
  workflow_dispatch:
permissions:
  contents: read
  actions: read
  issues: write
jobs:
  probe:
    runs-on: ubuntu-22.04
    steps:
      - run: echo ok

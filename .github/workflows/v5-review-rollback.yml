name: V5 Review Rollback H2

on:
  pull_request:
    branches: [main]
    types: [synchronize]

permissions:
  contents: read

jobs:
  gate-only:
    name: CI Gate
    runs-on: ubuntu-22.04
    steps:
      - run: echo "H2 CI Gate success without new review."
